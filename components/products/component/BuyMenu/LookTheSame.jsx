import React from 'react'
import { connect } from 'react-redux';

const LookTheSame = () => {
    return (
        <div>
            123
        </div>
    )
}

let mapStateToProps = (state) => {
    return {
      
    };
};

export default connect(mapStateToProps)(LookTheSame);